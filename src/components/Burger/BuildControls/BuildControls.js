import React from 'react';
import classes from './BuildControls.css';

const buildControls = (props) => (
    <div className={classes.BuildControls}>
        <BuildControl label='Bouton'></BuildControl>
    </div>
);

export default buildControls;
